{% extends 'base.html.twig' %}

{% block title %}{{categorie.name}}{% endblock %}

{% block body %}
    <main class="container mt-4">
        <h3 class="pb-4 mb-4 fst-italic border-bottom">{{categorie.name}}</h3>
        <div>
            {# {% for cat in allCategories %} 
                <li>
                    <a href="{{ path ('app_category_show', {slug: cat.slug}) }}">{{ cat.name }}</a>
                </li>
            {% endfor %} #}
            {% for categorie in categories %}
                <li>
                    <a href="{{ path ('app_category_show_prest', {slug: categorie.slug}) }}">{{ categorie.name }}</a>
                </li>
            {% endfor %}

        </div>
        <div id="prest-list" class="service-reveal">
            {% include "pages/prest/_single_prest.html.twig" with {prests: prests} %}
        </div>
   
    </main>
{% endblock %}
